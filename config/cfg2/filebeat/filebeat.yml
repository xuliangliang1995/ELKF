filebeat.inputs:
  - type: log
    enabled: true
    paths:
      # 读取 docker 容器的日志
      - '/var/lib/docker/containers/*/*.log'
    # json 解析
    json.keys_under_root: true
    json.add_error_key: true

output.logstash:
  enabled: true
  hosts: ["logstash:5044"]

